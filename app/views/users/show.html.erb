<section>
    <%= render partial: 'ideas/form', locals: { submit: "ADD NEW IDEA"} %>  
</section>

<section>
  <ul class="table-view">
    <li class="table-view-cell table-view-divider">
      <%= @user.ideas.count %> Ideas
    </li>
    <% if @user.ideas.any? %>
     <%= render @ideas %>
    <% end %>
  </ul>
</section>